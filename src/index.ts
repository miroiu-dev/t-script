/*
    @data-types  @example
    number       1, 4, 2.5
    string       "hello"
    boolean      true, false 
    array        [1, 2, 3] --- same type only 
    object       { }
    null

    const x: number = 15;
    var x: number = 10;
    const x: Array<number> = [1, 2, 3];

    x = 5;
    ++x;
    x--;

    fun sum(a: number, b: number): number {
        return a + b;
    }

    x = 15;
*/
